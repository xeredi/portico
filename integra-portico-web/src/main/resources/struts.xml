<!DOCTYPE struts PUBLIC
    "-//Apache Software Foundation//DTD Struts Configuration 2.3//EN"
    "http://struts.apache.org/dtds/struts-2.3.dtd">
<struts>
    <constant name="struts.configuration.xml.reload" value="false" />
    <constant name="struts.convention.classes.reload" value="false" />
    <constant name="struts.custom.i18n.resources" value="messages" />
    <constant name="struts.devMode" value="false" />
    <constant name="struts.el.throwExceptionOnFailure" value="true" />
    <constant name="struts.freemarker.mru.max.strong.size" value="250" />
    <constant name="struts.freemarker.templatesCache.updateDelay" value="180000" />
    <constant name="struts.i18n.encoding" value="UTF-8" />
    <constant name="struts.i18n.reload" value="false" />
    <constant name="struts.ognl.enableExpressionCache" value="true" />
    <constant name="struts.ognl.logMissingProperties" value="true" />
    <constant name="struts.serve.static" value="false" />
    <constant name="struts.ui.templateSuffix" value="java" /><!-- -->
    <constant name="struts.ui.theme" value="simple" />
    <constant name="struts.xslt.nocache" value="false" />

    <!-- REST -->
    <!-- <constant name="struts.action.extension" value="xhtml,,xml,json,action" /> <constant name="struts.mapper.class"
        value="org.apache.struts2.dispatcher.mapper.PrefixBasedActionMapper" /> <constant name="struts.mapper.prefixMapping" value="/rest:rest,:struts"
        /> <constant name="struts.convention.action.suffix" value="Controller" /> <constant name="struts.convention.action.mapAllMatches"
        value="true" /> <constant name="struts.convention.default.parent.package" value="rest-default" /> <constant name="struts.convention.package.locators"
        value="xeredi" /> -->

    <package name="integra" namespace="/" extends="json-default">
        <interceptors>
            <!-- <interceptor name="loginInterceptor" class="xeredi.integra.http.controller.interceptor.LoginInterceptor"
                /> -->

            <interceptor-stack name="integraStack">
                <!-- <interceptor-ref name="loginInterceptor" /> -->
                <interceptor-ref name="timer" />
                <interceptor-ref name="logger" />
                <interceptor-ref name="json">
                    <param name="excludeNullProperties">true</param>
                </interceptor-ref>
                <interceptor-ref name="basicStack" />
            </interceptor-stack>
        </interceptors>

        <default-interceptor-ref name="integraStack"></default-interceptor-ref>

        <global-results>
            <result name="login" type="redirectAction">
                <param name="actionName">usuario-acceso</param>
            </result>
            <result name="404">/WEB-INF/content/error/404.jsp</result>
            <result name="500">/WEB-INF/content/error/500.jsp</result>
        </global-results>

        <global-exception-mappings>
            <exception-mapping result="404" exception="xeredi.util.exception.InstanceNotFoundException" />
            <exception-mapping result="500" exception="xeredi.util.exception.InternalErrorException" />
        </global-exception-mappings>
    </package>
</struts>
