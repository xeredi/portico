package xeredi.integra.proceso.servicio.escala;

// TODO: Auto-generated Javadoc
/**
 * The Enum BermanConstante.
 */
public enum BermanConstante {

    /** The aai. */
    AAI,
    /** The acb. */
    ACB,
    /** The zie. */
    ZIE,
    /** The zin. */
    ZIN,
    /** The zfu. */
    ZFU,
    /** The znf. */
    ZNF,
    /** The zcc. */
    ZCC,
    /** The znc. */
    ZNC,
    /** The zga. */
    ZGA,
    /** The zng. */
    ZNG,
    /** The zrl. */
    ZRL,
    /** The zrs. */
    ZRS,
    /** The zer. */
    ZER,
    /** The zte. */
    ZTE,
    /** The zts. */
    ZTS,
    /** The zwe. */
    ZWE,
    /** The zws. */
    ZWS,
    /** The zdp. */
    ZDP,
    /** The zpe. */
    ZPE,
    /** The zps. */
    ZPS,
    /** The cv. */
    CV,
    /** The ov. */
    OV,
    /** The vm. */
    VM,
    /** The zio. */
    ZIO,
    /** The zic. */
    ZIC,
    /** The zsc. */
    ZSC,
    /** The zcs. */
    ZCS,
    /** The zgs. */
    ZGS,
    /** The zsb. */
    ZSB,
    /** The zae. */
    ZAE,
    /** The zan. */
    ZAN,
    /** The zme. */
    ZME,
    /** The znm. */
    ZNM,
    /** The zmd. */
    ZMD,
    /** The wm. */
    WM,
    /** The lm. */
    LM,
    /** The hm. */
    HM,
    /** The aam. */
    AAM,
    /** The zdw. */
    ZDW,
    /** The ht. */
    HT,
    /** The zmp. */
    ZMP,
    /** The zvc. */
    ZVC,
    /** The zvm. */
    ZVM,
    /** The ztl. */
    ZTL,
    /** The zhe. */
    ZHE,
    /** The zvl. */
    ZVL,
    /** The aae. */
    AAE,
    /** The lao. */
    LAO,
    /** The wt. */
    WT,
    /** The abs. */
    ABS,
    /** The zcd. */
    ZCD,
    /** The zdd. */
    ZDD,
    /** The zbd. */
    ZBD,
    /** The zac. */
    ZAC,
    /** The Z2 c. */
    Z2C,
    /** The zob. */
    ZOB,
    /** The zcl. */
    ZCL,
    /** The zcv. */
    ZCV,
    /** The zci. */
    ZCI,
    /** The zll. */
    ZLL,
    /** The zlv. */
    ZLV,
    /** The zli. */
    ZLI,
    /** The zoi. */
    ZOI,
    /** The l. */
    L,
    /** The v. */
    V,
    /** The i. */
    I,
    /** The zni. */
    ZNI,
    /** The zca. */
    ZCA,
    /** The c. */
    C,
    /** The zad. */
    ZAD,
    /** The zsa. */
    ZSA,
    /** The zsh. */
    ZSH,
    /** The zse. */
    ZSE,
    /** The zep. */
    ZEP,
    /** The zel. */
    ZEL,
    /** The zot. */
    ZOT,
    /** The zct. */
    ZCT,
    /** The zop. */
    ZOP,
    /** The zab. */
    ZAB,
    /** The zce. */
    ZCE, U, CT, AAV

    ;
}
