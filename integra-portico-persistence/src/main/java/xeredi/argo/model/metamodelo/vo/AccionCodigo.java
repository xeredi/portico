package xeredi.argo.model.metamodelo.vo;

// TODO: Auto-generated Javadoc
/**
 * The Enum AccionCodigo.
 */
public enum AccionCodigo {

    /** The index. */
    index,
    /** The list. */
    list,
    /** The detail. */
    detail,
    /** The edit. */
    edit,
    /** The create. */
    create,
    /** The duplicate. */
    duplicate,
    /** The remove. */
    remove,

    /** The load. */
    load,

    /** The duplicate_version. FIXME Ver si hace falta!!!! */
    duplicate_version,
    /** The print. FIXME Ver si hace falta!!!! */
    print,
    /** The xls_export. FIXME Ver si hace falta!!!! */
    xls_export,
    /**
     * Importacion de ficheros, utilizado en manifiestos de pesca (fichero plano), manifiestos de mercancía
     * (edifact) y escalas (edifact).
     */
    file_import,

    /** The export. */
    export,

    /** The ittr list. */
    ittrList,
    /** The ittr detail. */
    ittrDetail,
    /** The dep list. */
    depList,

    ;
}
