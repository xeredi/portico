package xeredi.argo.model.metamodelo.vo;

// TODO: Auto-generated Javadoc
/**
 * The Enum Entidad.
 */
public enum Entidad {

    /** The actividad pesquera. */
    ACTIVIDAD_PESQUERA(23000L), /** The acuerdo. */
    ACUERDO(20003L), /** The agregacion escala. */
    AGREGACION_ESCALA(23001L), /** The agregacion superficie. */
    AGREGACION_SUPERFICIE(23002L), /** The alineacion. */
    ALINEACION(20109L), /** The amarre dep. */
    AMARRE_DEP(20118L), /** The amarre dep srv. */
    AMARRE_DEP_SRV(21021L), /** The amarre punto red. */
    AMARRE_PUNTO_RED(24016L), /** The amarre servicio deportivo. */
    AMARRE_SERVICIO_DEPORTIVO(24015L), /** The area geografica. */
    AREA_GEOGRAFICA(20012L), /** The area mundial. */
    AREA_MUNDIAL(20006L), /** The arte pesca. */
    ARTE_PESCA(20041L), /** The asignacion amarre. */
    ASIGNACION_AMARRE(21010L), /** The atraque. */
    ATRAQUE(22011L), /** The autoridad portuaria. */
    AUTORIDAD_PORTUARIA(20107L), /** The avituallamiento. */
    AVITUALLAMIENTO(23003L), /** The baremo fp. */
    BAREMO_FP(20071L), /** The bl. */
    BL(22003L), /** The bonificacion fp. */
    BONIFICACION_FP(20070L), /** The buque. */
    BUQUE(20005L), /** The buque capacidad. */
    BUQUE_CAPACIDAD(24001L), /** The buque certificado. */
    BUQUE_CERTIFICADO(24002L), /** The buque convenio. */
    BUQUE_CONVENIO(24004L), /** The buque subpuerto. */
    BUQUE_SUBPUERTO(24003L), /** The buque trafico. */
    BUQUE_TRAFICO(24000L), /** The buque fondeado atracado. */
    BUQUE_FONDEADO_ATRACADO(23004L), /** The buque pesca. */
    BUQUE_PESCA(20040L),

    /** The buque pesca srv. */
    BUQUE_PESCA_SRV(21020L), /** The comprador pesca. */
    COMPRADOR_PESCA(20108L), /** The contador. */
    CONTADOR(20113L), /** The convenio. */
    CONVENIO(20093L), /** The descuento adicional. */
    DESCUENTO_ADICIONAL(20068L), /** The embarcacion dep. */
    EMBARCACION_DEP(20120L), /** The embarcacion dep aut. */
    EMBARCACION_DEP_AUT(20121L), /** The equipamiento. */
    EQUIPAMIENTO(22005L), /** The escala. */
    ESCALA(21003L), /** The escala contador. */
    ESCALA_CONTADOR(22015L), /** The escala residuo. */
    ESCALA_RESIDUO(22016L), /** The especie pesca. */
    ESPECIE_PESCA(20038L), /** The estado expediente. */
    ESTADO_EXPEDIENTE(20081L), /** The familia especie pesca. */
    FAMILIA_ESPECIE_PESCA(20036L), /** The fase expediente. */
    FASE_EXPEDIENTE(20080L), /** The garantia expediente. */
    GARANTIA_EXPEDIENTE(20079L), /** The grupo arancelario. */
    GRUPO_ARANCELARIO(20045L), /** The grupo familia especie pesca. */
    GRUPO_FAMILIA_ESPECIE_PESCA(20035L), /** The grupo naturaleza. */
    GRUPO_NATURALEZA(20043L), /** The grupo nst. */
    GRUPO_NST(20047L), /** The grupo zona. */
    GRUPO_ZONA(20021L), /** The instalacion dep. */
    INSTALACION_DEP(20101L), /** The instalacion dep aut. */
    INSTALACION_DEP_AUT(20100L), /** The instalacion especial. */
    INSTALACION_ESPECIAL(20061L), /** The instruccion marcaje. */
    INSTRUCCION_MARCAJE(20060L), /** The manifiesto. */
    MANIFIESTO(21002L), /** The manifiesto consignatario. */
    MANIFIESTO_CONSIGNATARIO(22002L), /** The manifiesto pesca. */
    MANIFIESTO_PESCA(21001L), /** The marca vehiculo. */
    MARCA_VEHICULO(20059L), /** The mercancia. */
    MERCANCIA(20049L), /** The mercancias peligrosas. */
    MERCANCIAS_PELIGROSAS(20058L), /** The modo transporte edi. */
    MODO_TRANSPORTE_EDI(20057L), /** The modulo. */
    MODULO(20102L), /** The modulo batch. */
    MODULO_BATCH(20104L), /** The movimiento mercancia. */
    MOVIMIENTO_MERCANCIA(23005L), /** The movimiento mercancia eee. */
    MOVIMIENTO_MERCANCIA_EEE(23006L), /** The movimiento tipo buque eee. */
    MOVIMIENTO_TIPO_BUQUE_EEE(23007L), /** The muelle. */
    MUELLE(20091L), /** The municipio. */
    MUNICIPIO(20116L), /** The naturaleza. */
    NATURALEZA(20044L), /** The naviera. */
    NAVIERA(20090L), /** The ocupacion superficie. */
    OCUPACION_SUPERFICIE(21007L), /** The ocupacion superficie linea. */
    OCUPACION_SUPERFICIE_LINEA(22018L), /** The operacion atraque. */
    OPERACION_ATRAQUE(22012L), /** The organizacion. */
    ORGANIZACION(20010L), /** The organizacion subpuerto. */
    ORGANIZACION_SUBPUERTO(24010L), /** The organizacion tipo. */
    ORGANIZACION_TIPO(24011L), /** The pais. */
    PAIS(20004L), /** The partida. */
    PARTIDA(22004L), /** The partida documento. */
    PARTIDA_DOCUMENTO(22007L), /** The partida equipamiento. */
    PARTIDA_EQUIPAMIENTO(22006L), /** The partida im. */
    PARTIDA_IM(22008L), /** The partida mmpp. */
    PARTIDA_MMPP(22009L), /** The partida pesca. */
    PARTIDA_PESCA(22001L), /** The precinto equipamiento. */
    PRECINTO_EQUIPAMIENTO(22010L), /** The presentacion pesca. */
    PRESENTACION_PESCA(20031L), /** The provincia. */
    PROVINCIA(20062L), /** The punto red. */
    PUNTO_RED(20114L), /** The punto red organizacion. */
    PUNTO_RED_ORGANIZACION(24012L), /** The punto red tipo gasto. */
    PUNTO_RED_TIPO_GASTO(24013L), /** The punto red tipo lectura. */
    PUNTO_RED_TIPO_LECTURA(24014L), /** The receptor mercancia. */
    RECEPTOR_MERCANCIA(20056L), /** The recinto aduanero. */
    RECINTO_ADUANERO(20054L), /** The redes. */
    REDES(20110L), /** The registro tipo buque. */
    REGISTRO_TIPO_BUQUE(20083L), /** The registro tipo buque eee. */
    REGISTRO_TIPO_BUQUE_EEE(20082L), /** The servicio deportivo. */
    SERVICIO_DEPORTIVO(20099L), /** The servicio trafico. */
    SERVICIO_TRAFICO(20086L), /** The subgrupo. */
    SUBGRUPO(20046L), /** The subgrupo mercancia est. */
    SUBGRUPO_MERCANCIA_EST(20046L), /** The subpuerto. */
    SUBPUERTO(20063L), /** The subtipo manifiesto pesca. */
    SUBTIPO_MANIFIESTO_PESCA(20034L), /** The subtipo residuopesca. */
    SUBTIPO_RESIDUO(20089L), /** The suministro consumo. */
    SUMINISTRO_CONSUMO(21005L), /** The suministro consumo gasto. */
    SUMINISTRO_CONSUMO_GASTO(22013L), /** The suministro consumo lectura. */
    SUMINISTRO_CONSUMO_LECTURA(22014L), /** The suprabien. */
    SUPRABIEN(20078L), /** The terminal. */
    TERMINAL(20055L), /** The tipo actividad general concesional. */
    TIPO_ACTIVIDAD(20001L), /** The tipo actividad concesional. */
    TIPO_ACTIVIDAD_CONCESIONAL(20077L), /** The tipo actividad edi. */
    TIPO_ACTIVIDAD_EDI(20002L), /** The tipo actividad general concesional. */
    TIPO_ACT_GRAL_CONCESIONAL(20076L), /** The tipo alineacion. */
    TIPO_ALINEACION(20015L), /** The tipo amarre. */
    TIPO_AMARRE(20098L), /** The tipo atraque. */
    TIPO_ATRAQUE(20096L), /** The tipo atraque edi. */
    TIPO_ATRAQUE_EDI(20115L), /** The tipo bien concesional. */
    TIPO_BIEN_CONCESIONAL(20075L), /** The tipo bulto. */
    TIPO_BULTO(20053L), /** The tipo buque eee. */
    TIPO_BUQUE(20009L), /** The tipo buque edi. */
    TIPO_BUQUE_EDI(20095L), /** The tipo buque eee. */
    TIPO_BUQUE_EEE(20007L), /** The tipo buque est. */
    TIPO_BUQUE_EST(20008L), /** The tipo buque gt. */
    TIPO_BUQUE_GT(20085L), /** The tipo buque gt eee. */
    TIPO_BUQUE_GT_EEE(20117L), /** The tipo buque pesca. */
    TIPO_BUQUE_PESCA(20039L), /** The tipo canon. */
    TIPO_CANON(20069L), /** The tipo captura pesca. */
    TIPO_CAPTURA_PESCA(20037L), /** The tipo certificado. */
    TIPO_CERTIFICADO(20094L), /** The tipo contador. */
    TIPO_CONTADOR(20026L), /** The tipo documento. */
    TIPO_DOCUMENTO(20103L), /** The tipo documento aeat. */
    TIPO_DOCUMENTO_AEAT(20052L), /** The tipo embarcacion. */
    TIPO_EMBARCACION(20097L), /** The tipo equipamiento. */
    TIPO_EQUIPAMIENTO(20051L), /** The tipo error edi. */
    TIPO_ERROR_EDI(20067L), /** The tipo exencion. */
    TIPO_EXENCION(20066L), /** The tipo expediente. */
    TIPO_EXPEDIENTE(20074L), /** The tipo gasto. */
    TIPO_GASTO(20025L), /** The tipo iva. */
    TIPO_IVA(20011L), /** The tipo lectura. */
    TIPO_LECTURA(20119L), /** The tipo manifiesto pesca. */
    TIPO_MANIFIESTO_PESCA(20033L), /** The tipo maniobra remolque. */
    TIPO_MANIOBRA_REMOLQUE(20029L), /** The tipo medio amarre. */
    TIPO_MEDIO_AMARRE(20016L), /** The tipo medio remolque. */
    TIPO_MEDIO_REMOLQUE(20027L), /** The tipo mercancia. */
    TIPO_MERCANCIA(20112L), /** The tipo mercancia est. */
    TIPO_MERCANCIA_EST(20042L), /** The tipo movimiento amarre. */
    TIPO_MOVIMIENTO_AMARRE(20017L), /** The tipo movimiento remolque. */
    TIPO_MOVIMIENTO_REMOLQUE(20028L), /** The tipo navegacion. */
    TIPO_NAVEGACION(20065L), /** The tipo operacion mercancia. */
    TIPO_OPERACION_MERCANCIA(20064L), /** The tipo operacion pesca. */
    TIPO_OPERACION_PESCA(20032L), /** The tipo operacion superficie. */
    TIPO_OPERACION_SUPERFICIE(20023L), /** The tipo organizacion. */
    TIPO_ORGANIZACION(20106L), /** The tipo prestador servicio. */
    TIPO_PRESTADOR_SERVICIO(20105L), /** The tipo residuo. */
    TIPO_RESIDUO(20088L), /** The tipo servicio trafico. */
    TIPO_SERVICIO_TRAFICO(20087L), /** The tipo suministro. */
    TIPO_SERV_TIPO_TRAF(24020L),
    /** The tipo suministro. */
    TIPO_SUMINISTRO(20024L), /** The tipo superficie. */
    TIPO_SUPERFICIE(20020L), /** The tipo superficie bien. */
    TIPO_SUPERFICIE_BIEN(20073L), /** The tipo usuario. */
    TIPO_USUARIO(20019L), /** The tipo via. */
    TIPO_VIA(20018L), /** The unidad carga. */
    UNIDAD_CARGA(20048L), /** The unidad superficie. */
    UNIDAD_SUPERFICIE(20022L), /** The unlocode. */
    UNLOCODE(20014L), /** The zona costera eee. */
    ZONA_COSTERA_EEE(20013L), /** The zona deposito. */
    ZONA_DEPOSITO(20111L), /** The zona pesca. */
    ZONA_PESCA(20030L), /** The zona portuaria. */
    ZONA_PORTUARIA(20072L), ;

    /** The id. */
    private final long id;

    /**
     * Instantiates a new entidad.
     *
     * @param aid
     *            the aid
     */
    private Entidad(final long aid) {
        id = aid;
    }

    /**
     * Gets the id.
     *
     * @return the id
     */
    public long getId() {
        return id;
    }
}
