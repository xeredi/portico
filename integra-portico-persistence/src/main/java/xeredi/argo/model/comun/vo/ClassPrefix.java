package xeredi.argo.model.comun.vo;

// TODO: Auto-generated Javadoc
/**
 * The Enum ClassPrefix.
 */
public enum ClassPrefix {

    /** The conf. */
    conf,
    /** The m 18 n. */
    m18n,
    /** The usro. */
    usro,
    /** The grpo. */
    grpo,
    /** The fncd. */
    fncd,
    /** The mdlo. */
    mdlo,
    /** The acbs. */
    acbs,
    /** The acen. */
    acen,
    /** The aces. */
    aces,
    /** The trmt. */
    trmt,
    /** The trtd. */
    trtd,
    /** The sprt. */
    sprt,
    /** The prto. */
    prto,
    /** The item. */
    item,
    /** The prmt. */
    prmt,
    /** The prvr. */
    prvr,
    /** The prdt. */
    prdt,
    /** The sprm. */
    sprm,
    /** The spvr. */
    spvr,
    /** The srvc. */
    srvc,
    /** The srsc. */
    srsc,
    /** The srdt. */
    srdt,
    /** The ssrv. */
    ssrv,
    /** The ssdt. */
    ssdt,
    /** The ssss. */
    ssss,
    /** The ittr. */
    ittr,
    /** The estd. */
    estd,
    /** The esdt. */
    esdt,
    /** The pepr. */
    pepr,
    /** The cdms. */
    cdms,
    /** The enti. */
    enti,
    /** The enen. */
    enen,
    /** The tpsr. */
    tpsr,
    /** The tpss. */
    tpss,
    /** The tppr. */
    tppr,
    /** The tpsp. */
    tpsp,
    /** The tpes. */
    tpes,
    /** The cmag. */
    cmag,
    /** The entd. */
    entd,
    /** The engd. */
    engd,
    /** The tpdt. */
    tpdt,
    /** The cdrf. */
    cdrf,
    /** The vlrc. */
    vlrc,
    /** The vlrt. */
    vlrt,
    /** The vlrl. */
    vlrl,
    /** The vlrd. */
    vlrd,
    /** The fctr. */
    fctr,
    /** The fctl. */
    fctl,
    /** The fctd. */
    fctd,
    /** The crgo. */
    crgo,
    /** The crgv. */
    crgv,
    /** The rgla. */
    rgla,
    /** The rglv. */
    rglv,
    /** The rgin. */
    rgin, rgiv,
    /** The aspc. */
    aspc,
    /** The aspv. */
    aspv,
    /** The ascr. */
    ascr, ascv,
    /** The fcan. */
    fcan,
    /** The fcrc. */
    fcrc,
    /** The fcsr. */
    fcsr,
    /** The fcdr. */
    vldr,
    /** The fcdr. */
    fcdr,
    /** The prbt. */
    prbt,
    /** The prmn. */
    prmn,

    // PARA BORRAR!!!!
    /** The actr. */
    actr,

    ;
}
