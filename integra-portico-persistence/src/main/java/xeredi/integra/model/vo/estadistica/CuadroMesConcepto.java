package xeredi.integra.model.vo.estadistica;

// TODO: Auto-generated Javadoc
/**
 * The Enum CuadroMesConcepto.
 */
public enum CuadroMesConcepto {

    /** The pescaf. */
    PESCAF,
    /** The avppet. */
    AVPPET,
    /** The avotro. */
    AVOTRO,
    /** The buquni. */
    BUQUNI,
    /** The buqgt. */
    BUQGT,
    /** The crubuq. */
    CRUBUQ,
    /** The glpetr. */
    GLPETR,
    /** The glgasn. */
    GLGASN,
    /** The glpref. */
    GLPREF,
    /** The glotro. */
    GLOTRO,
    /** The gsiesp. */
    GSIESP,
    /** The gsnies. */
    GSNIES,
    /** The mg. */
    MG,
    /** The pasaje. */
    PASAJE,
    /** The VE t2. */
    VET2,
    /** The pascru. */
    PASCRU,
    /** The cnumca. */
    CNUMCA,
    /** The ctonca. */
    CTONCA,
    /** The cnumva. */
    CNUMVA,
    /** The ctonva. */
    CTONVA,
    /** The cteus. */
    CTEUS,
    /** The rrteus. */
    RRTEUS,
    /** The rrtonc. */
    RRTONC,
    /** The rrtono. */
    RRTONO,
    /** The traloc. */
    TRALOC,
    /** The mconv. */
    MCONV;
}
