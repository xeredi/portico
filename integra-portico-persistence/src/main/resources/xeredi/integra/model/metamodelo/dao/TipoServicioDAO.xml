<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="xeredi.integra.model.metamodelo.dao.TipoServicioDAO">
    <resultMap type="TipoServicioVO" id="ResultMap" extends="xeredi.integra.model.metamodelo.dao.EntidadDAO.ResultMap">
        <result column="tpsr_es_temporal" property="temporal" />
        <result column="tpsr_es_facturable" property="facturable" />

        <association property="tpdtEstado" javaType="TipoDatoVO">
            <result column="tpsr_tpdt_estado_pk" property="id" />
            <result column="tpdt_nombre" property="nombre" />
        </association>
    </resultMap>

    <sql id="SelectPrefix">
    <![CDATA[
        SELECT
            enti.enti_pk, enti.enti_nombre, enti.enti_codigo, enti.enti_tipo
            , enti.enti_cmd_alta, enti.enti_cmd_baja, enti.enti_cmd_edicion, enti.enti_cmd_duplicado
            , tpsr.tpsr_es_temporal, tpsr.tpsr_es_facturable
            , tpsr.tpsr_tpdt_estado_pk, tpdt.tpdt_nombre
        FROM
            tbl_tipo_servicio_tpsr tpsr
            INNER JOIN tbl_entidad_enti enti ON
                enti.enti_pk = tpsr.tpsr_pk
            LEFT JOIN tbl_tipo_dato_tpdt tpdt ON
                tpdt.tpdt_pk = tpsr.tpsr_tpdt_estado_pk
    ]]>
    </sql>

    <sql id="SelectWhere">
        <where>
            <if test="id != null">
                AND enti.enti_pk = #{id}
            </if>
            <if test="ids != null">
                AND enti.enti_pk IN
                <foreach collection="ids" item="item" open="(" close=")" separator=", ">#{item}</foreach>
            </if>
            <if test="nombre != null and !nombre.empty">
                AND enti.enti_nombre LIKE #{nombre}
            </if>
            <if test="codigo != null and !codigo.empty">
                AND enti.enti_codigo LIKE #{codigo}
            </if>
            <if test="facturable != null">
                AND tpsr.tpsr_es_facturable = #{facturable, jdbcType=INTEGER, javaType=Boolean}
            </if>
        </where>
    </sql>

    <select id="selectList" parameterType="TipoServicioCriterioVO" resultMap="ResultMap">
        <include refid="SelectPrefix" />
        <include refid="SelectWhere" />
        <![CDATA[
        ORDER BY enti.enti_nombre, enti.enti_pk
        ]]>
    </select>

    <select id="count" resultType="int">
        <![CDATA[
        SELECT COUNT(1)
        FROM
            tbl_tipo_servicio_tpsr tpsr
            INNER JOIN tbl_entidad_enti enti ON
                enti.enti_pk = tpsr.tpsr_pk
        ]]>
        <include refid="SelectWhere" />
    </select>

    <select id="select" resultMap="ResultMap" parameterType="Long">
        <include refid="SelectPrefix" />
        <where>
        <![CDATA[
        enti.enti_pk = #{value}
        ]]>
        </where>
    </select>

    <insert id="insert" parameterType="TipoServicioVO">
        <![CDATA[
        INSERT INTO tbl_tipo_servicio_tpsr(tpsr_pk, tpsr_es_temporal, tpsr_es_facturable, tpsr_tpdt_estado_pk)
        VALUES (#{id}
        	, #{temporal, jdbcType=INTEGER, javaType=Boolean}
        	, #{facturable, jdbcType=INTEGER, javaType=Boolean}
        	, #{tpdtEstado.id})
        ]]>
    </insert>

    <update id="update" parameterType="TipoServicioVO">
        <![CDATA[
        UPDATE tbl_tipo_servicio_tpsr SET
            tpsr_es_temporal = #{temporal, jdbcType=INTEGER, javaType=Boolean}
            , tpsr_es_facturable = #{facturable, jdbcType=INTEGER, javaType=Boolean}
            , tpsr_tpdt_estado_pk = #{tpdtEstado.id}
        WHERE tpsr_pk = #{id}
        ]]>
    </update>
</mapper>
