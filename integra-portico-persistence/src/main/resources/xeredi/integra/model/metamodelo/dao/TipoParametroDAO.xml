<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="xeredi.integra.model.metamodelo.dao.TipoParametroDAO">
    <resultMap type="TipoParametroVO" id="ResultMap"
        extends="xeredi.integra.model.metamodelo.dao.EntidadDAO.ResultMap">
        <result column="tppr_es_i18n" property="i18n" />
        <result column="tppr_es_tmp_exp" property="tempExp" />
        <result column="tppr_tpdt_pk" property="tpdtNombreId" />
    </resultMap>

    <sql id="SelectPrefix">
    <![CDATA[
        SELECT
            enti_pk, enti_nombre, enti_codigo, enti_tipo
            , enti_cmd_alta, enti_cmd_baja, enti_cmd_edicion, enti_cmd_duplicado
            , tppr_es_i18n, tppr_es_tmp_exp, tppr_tpdt_pk
        FROM
            tbl_tipo_parametro_tppr
            INNER JOIN tbl_entidad_enti ON
                enti_pk = tppr_pk
    ]]>
    </sql>

    <sql id="SelectWhere">
        <where>
            <if test="ids != null">
                AND enti_pk IN
                <foreach collection="ids" item="item" open="(" close=")" separator=", ">#{item}
                </foreach>
            </if>
            <if test="nombre != null and !nombre.empty">
                AND enti_nombre LIKE #{nombre}
            </if>
            <if test="codigo != null and !codigo.empty">
                AND enti_codigo LIKE #{codigo}
            </if>
        </where>
    </sql>

    <select id="selectList" parameterType="TipoParametroCriterioVO" resultMap="ResultMap">
        <include refid="SelectPrefix" />
        <include refid="SelectWhere" />

        ORDER BY enti_nombre, enti_pk
    </select>

    <select id="count" resultType="int">
        <![CDATA[
        SELECT COUNT(1)
        FROM
            tbl_tipo_parametro_tppr
            INNER JOIN tbl_entidad_enti ON
                enti_pk = tppr_pk
        ]]>
        <include refid="SelectWhere" />
    </select>

    <select id="select" resultMap="ResultMap" parameterType="Long">
        <include refid="SelectPrefix" />
        <where>
            enti_pk = #{value}
        </where>
    </select>

    <select id="selectLabelValues" parameterType="TipoParametroCriterioVO" resultType="LabelValueVO">
        <![CDATA[
        SELECT
            enti_pk AS value
            , enti_nombre AS label
        FROM
            tbl_tipo_parametro_tppr
            INNER JOIN tbl_entidad_enti ON
                enti_pk = tppr_pk
        ]]>
        <include refid="SelectWhere" />
        ORDER BY enti_nombre
    </select>

    <insert id="insert" parameterType="TipoParametroVO">
        <![CDATA[
        INSERT INTO tbl_tipo_parametro_tppr(tppr_pk, tppr_es_i18n, tppr_es_tmp_exp, tppr_tpdt_pk)
        VALUES (#{id}, #{i18n, jdbcType=INTEGER, javaType=Boolean}, #{tempExp, jdbcType=INTEGER, javaType=Boolean}, #{tpdtNombreId})
        ]]>
    </insert>

    <update id="update" parameterType="TipoParametroVO">
        <![CDATA[
        UPDATE tbl_tipo_parametro_tppr SET
            tppr_es_i18n = #{i18n, jdbcType=INTEGER, javaType=Boolean}
            , tppr_es_tmp_exp = #{tempExp, jdbcType=INTEGER, javaType=Boolean}
            , , tppr_tpdt_pk = #{tpdtNombreId}
        WHERE tppr_pk = #{id}
        ]]>
    </update>
</mapper>
